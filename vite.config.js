import { defineConfig } from "vite";

export default defineConfig({
  // solo se usa cuando se quiere direccionar el nombre del archivo de github, debe de ser identico al nombre del repositorio
  base: '/todo-js-example-vite/'
});